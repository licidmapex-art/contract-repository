# Netlify configuration for Contract Repository (Next.js)

[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["pdf-parse", "googleapis"]
